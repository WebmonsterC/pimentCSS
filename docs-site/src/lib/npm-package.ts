/** Published npm package (registry name). */
export const NPM_PACKAGE_NAME = 'pimentcss-design-system';

export const NPM_PACKAGE_URL = `https://www.npmjs.com/package/${NPM_PACKAGE_NAME}`;

export const NPM_CDN_JSdelivr = `https://cdn.jsdelivr.net/npm/${NPM_PACKAGE_NAME}@1/dist/pimentcss.min.css`;

export const NPM_CDN_UNPKG = `https://unpkg.com/${NPM_PACKAGE_NAME}@1/dist/pimentcss.min.css`;
