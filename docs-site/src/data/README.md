# Documentation metadata (Astro site)

| File | Role |
|------|------|
| `nav.json` | Sidebar sections, labels, search keywords, version badge |
| `pages.json` | Page titles, leads, breadcrumbs, `static` keys, optional `componentSelector` |

Regenerate English leads (without overwriting custom fields): `npm run sync:doc-metadata`
